import { DateDelayTodayPipe } from './date-delay-today.pipe';

describe('DateDelayTodayPipe', () => {
  it('create an instance', () => {
    const pipe = new DateDelayTodayPipe();
    expect(pipe).toBeTruthy();
  });
});
