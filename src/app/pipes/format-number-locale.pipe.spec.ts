import { FormatNumberLocalePipe } from './format-number-locale.pipe';

describe('FormatNumberLocalePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatNumberLocalePipe();
    expect(pipe).toBeTruthy();
  });
});
