import { FormatDureePipe } from './format-duree.pipe';

describe('FormatDureePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDureePipe();
    expect(pipe).toBeTruthy();
  });
});
