import { FormatVisionnementPipe } from './format-visionnement.pipe';

describe('FormatVisionnementPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatVisionnementPipe();
    expect(pipe).toBeTruthy();
  });
});
